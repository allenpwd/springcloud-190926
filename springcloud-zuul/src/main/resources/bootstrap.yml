#测试动态路由，TODO没有效果
spring:
  cloud:
    config:
      name: zuulConfig #需要从github上读取的资源名称，不用加上yml等资源后缀
      profile: dev #本次访问的配置项
      label: master
      uri: http://localhost:7004 #springcloud config服务端地址